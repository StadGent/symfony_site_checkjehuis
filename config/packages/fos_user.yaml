fos_user:
    db_driver: orm # other valid values are 'mongodb', 'couchdb' and 'propel'
    firewall_name: main
    user_class: App\Entity\User
    from_email:
        address: "%env(FOS_USER_FROM_EMAIL_ADDRESS)%"
        sender_name: "%%env(FOS_USER_FROM_SENDER_NAME)%"
